// This file is simplified as configuration is now handled by OpenAIClient
// and settings are read directly from localStorage.
// We can keep it for potential future app-wide configs.

export const AppConfig = {
    // Example of a future config
    // THEME: 'dark'
};
